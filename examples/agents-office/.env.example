# Ключ Anthropic. Без него панель работает в демо-режиме.
ANTHROPIC_API_KEY=

# Название магазина в шапке панели.
AO_SHOP=Vitaflow · американские витамины

# Модель. По умолчанию claude-opus-5.
AO_MODEL=claude-opus-5

# Глубина проработки: low | medium | high | xhigh | max
AO_EFFORT=high

# Сколько шагов с инструментами разрешаем агенту за одну задачу
AO_MAX_STEPS=8

# Сеть. Для запуска за nginx оставьте 127.0.0.1.
AO_HOST=127.0.0.1
PORT=8787

# Пароль на вход в панель. Пустое значение = без пароля (только для localhost!).
AO_TOKEN=

# Сколько задач выполняется одновременно.
AO_CONCURRENCY=2

# Где хранить журнал и задачи.
AO_DATA_DIR=./data

# Управляющий: проверяет работу до того, как она дойдёт до владельца.
# off — выключить проверку (дешевле, но без контроля).
AO_REVIEW=on
AO_REVIEW_ROUNDS=1

# Дешёвая модель для служебных вызовов: выбор исполнителя и проверка.
AO_MODEL_SMALL=claude-haiku-4-5
